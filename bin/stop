#!/usr/bin/env node

var stop = require('../');
var app = stop.getServer(process.cwd());

var port = 3000;
require('http').createServer(app).listen(port, function () {
  console.log('listening on part ' + port);
  require('open')('http://localhost:' + port);
});